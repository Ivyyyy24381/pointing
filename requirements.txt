# Pointing Gesture Analysis Pipeline - Requirements

# ⚠️ IMPORTANT: Python Version Compatibility
# MediaPipe requires Python 3.8 - 3.12 (NOT 3.13+)
# If you have Python 3.13, create a virtual environment with Python 3.11 or 3.12:
#   python3.12 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# Core dependencies
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Step 0: Data Loading
pyrealsense2>=2.54.0  # Optional: For live RealSense camera capture

# Step 0: Target Detection
ultralytics>=8.0.0  # YOLOv8 for cup/target detection
torch>=2.0.0  # Required by YOLO
torchvision>=0.15.0

# Step 2: Skeleton Extraction
mediapipe>=0.10.0  # Human pose detection

# Step 3: Subject Extraction (Optional)
# SAM2 - Install from source:
# git clone https://github.com/facebookresearch/segment-anything-2.git thirdparty/sam2
# cd thirdparty/sam2 && pip install -e .

# Step 4: Calculation
scipy>=1.10.0  # For distance calculations

# Visualization
matplotlib>=3.7.0
open3d>=0.17.0  # For point cloud visualization

# GUI
tkinter  # Usually included with Python

# Development
pytest>=7.0.0  # For testing
